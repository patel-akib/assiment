/*Develop a program that converts temperature from degrees Celsius to Fahrenheit using the
formula: °F = (9/5 × °C) + 32*/
#include<stdio.h>
int main(){
	float celsius,i,j,k;
	printf("The temperature in celsius:");
	scanf("%f",&celsius);
	°F = (9/5 × °C) + 32);
	printf("The temperature in Fahrenheit is %f",i,j);
}
